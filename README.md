# supermarket-billing-system-java-mysql
A console-based Supermarket Billing System built with Core Java, JDBC, and MySQL using the DAO pattern. Manages items by category, generates itemized bills, and tracks out-of-stock products.
